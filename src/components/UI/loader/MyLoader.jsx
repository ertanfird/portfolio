import React from 'react';
import classes from './MyLoader.module.sass'

const MyLoader = ({}) => {
    return (
        <div className={classes.MyLoader}>

        </div>
    );
}

export default MyLoader;
